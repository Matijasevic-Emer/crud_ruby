<section class="container mx-auto p-4">
  <h2 class="text-3xl font-bold mb-4">Aprende ahora!!</h2>
  <div class="my-3">
    <h3 class="text-2xl font-bold"><%= @course.title %></h3>
    <p class="text-gray-600"><%= @course.description %></p>
    <p class="text-gray-600">Published: <%= @course.published ? 'Yes' : 'No' %></p>
    <p class="text-gray-600">Visible: <%= @course.visible ? 'Yes' : 'No' %></p>
  </div>
    <%= link_to "Volver", courses_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" %>
</section>

